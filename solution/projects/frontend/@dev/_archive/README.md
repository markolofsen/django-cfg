# `_archive/` — frozen history

**Never cite anything here as current status.** A document is here because it was
superseded or abandoned, kept only so a later reader can see what was believed
and why it changed.

A track that will not be built is `PARKED` here **with the reason**. Nothing is
deleted for being abandoned — a deleted plan gets proposed again by the next
person with the same idea.

| Track | Parked | Reason |
|---|---|---|
| — | Empty. | |

Checked 2026-09-11.
