# `blocked/` — waiting on something named

A track here names **the blocker, its owner, and the condition that unblocks it**.
Without all three it is not blocked, it is stalled — and stalled work belongs in
`planned/` or `_archive/`.

| Track | Blocker | Owner | Unblocks when |
|---|---|---|---|
| — | Empty. | | |

Checked 2026-09-11.
