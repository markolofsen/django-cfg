'use client';

// AUTO-GENERATED by openapi_processor / ts.hooks
// Create order
// DO NOT EDIT — re-run `make gen`.

import useSWRMutation from "swr/mutation";
import type { SWRMutationConfiguration } from "swr/mutation";
import { Trading } from "../../sdk.gen";
import type { TradingOrdersCreateData, TradingOrdersCreateResponses } from "../../types.gen";
import { OrderCreateSchema } from "../schemas/OrderCreate";

type Args = Omit<TradingOrdersCreateData, "url">;
type Result = TradingOrdersCreateResponses[keyof TradingOrdersCreateResponses];

export function useTradingOrdersCreate(
  config?: SWRMutationConfiguration<Result, Error, readonly ["trading_orders_create"], Args>,
) {
  return useSWRMutation<Result, Error, readonly ["trading_orders_create"], Args>(
    ["trading_orders_create"] as const,
    async (_key, { arg }: { arg: Args }) => {
      const res = await Trading.tradingOrdersCreate({ ...arg, throwOnError: true } as never);
      const data = res.data as Result;
      const parsed = OrderCreateSchema.safeParse(data);
      if (!parsed.success) {
        // Log first so the failure is always visible, even if the
        // event handler swallows it.
        console.warn(
          "[zod] response did not match schema",
          {
            operation: "trading_orders_create",
            method: "POST",
            path: "/apix/trading/orders/",
            issues: parsed.error.issues,
            data,
          },
        );
        if (typeof window !== "undefined") {
          try {
            window.dispatchEvent(
              new CustomEvent("zod-validation-error", {
                detail: {
                  operation: "trading_orders_create",
                  method: "POST",
                  path: "/apix/trading/orders/",
                  issues: parsed.error.issues,
                  data,
                  timestamp: new Date(),
                },
                bubbles: true,
                cancelable: false,
              }),
            );
          } catch {
            // Event dispatch is best-effort.
          }
        }
        return data;
      }
      return parsed.data as unknown as Result;
    },
    config,
  );
}
