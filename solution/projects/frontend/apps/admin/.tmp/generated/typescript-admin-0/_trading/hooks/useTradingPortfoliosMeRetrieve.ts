'use client';

// AUTO-GENERATED by openapi_processor / ts.hooks
// Get my portfolio
// DO NOT EDIT — re-run `make gen`.

import useSWR from "swr";
import type { SWRConfiguration } from "swr";
import { Trading } from "../../sdk.gen";
import type { TradingPortfoliosMeRetrieveData, TradingPortfoliosMeRetrieveResponses } from "../../types.gen";
import { PortfolioSchema } from "../schemas/Portfolio";

type Result = TradingPortfoliosMeRetrieveResponses[keyof TradingPortfoliosMeRetrieveResponses];

/**
 * Optional `enabled` flag — when `false`, the SWR key becomes `null` and
 * no request is made. Use for gated endpoints (auth-required, feature
 * flags, "fetch only when X is selected"). The flag is stripped before
 * being forwarded to `useSWR` so the underlying SWR config stays pristine.
 */
type HookConfig = SWRConfiguration<Result> & { enabled?: boolean };

export function useTradingPortfoliosMeRetrieve(
  args?: Omit<TradingPortfoliosMeRetrieveData, "url" | "body">,
  config?: HookConfig,
) {
  const { enabled = true, ...swrConfig } = config ?? {};
  const key = enabled ? ["trading_portfolios_me_retrieve", args ?? {}] as const : null;
  return useSWR<Result>(
    key,
    async () => {
      const res = await Trading.tradingPortfoliosMeRetrieve({ ...(args ?? {}), throwOnError: true } as never);
      const data = res.data as Result;
      const parsed = PortfolioSchema.safeParse(data);
      if (!parsed.success) {
        // Log first so the failure is always visible, even if the
        // event handler swallows it.
        console.warn(
          "[zod] response did not match schema",
          {
            operation: "trading_portfolios_me_retrieve",
            method: "GET",
            path: "/apix/trading/portfolios/me/",
            issues: parsed.error.issues,
            data,
          },
        );
        if (typeof window !== "undefined") {
          try {
            window.dispatchEvent(
              new CustomEvent("zod-validation-error", {
                detail: {
                  operation: "trading_portfolios_me_retrieve",
                  method: "GET",
                  path: "/apix/trading/portfolios/me/",
                  issues: parsed.error.issues,
                  data,
                  timestamp: new Date(),
                },
                bubbles: true,
                cancelable: false,
              }),
            );
          } catch {
            // Event dispatch is best-effort.
          }
        }
        return data;
      }
      return parsed.data as unknown as Result;
    },
    swrConfig,
  );
}
