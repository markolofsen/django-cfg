// Auto-generated by DjangoCFG - see CLAUDE.md
/**
 * * `market` - Market
 * * `limit` - Limit
 * * `stop_loss` - Stop Loss
 */
export enum OrderOrderType {
  MARKET = "market",
  LIMIT = "limit",
  STOP_LOSS = "stop_loss",
}

/**
 * * `buy` - Buy
 * * `sell` - Sell
 */
export enum OrderSide {
  BUY = "buy",
  SELL = "sell",
}

/**
 * * `pending` - Pending
 * * `filled` - Filled
 * * `cancelled` - Cancelled
 */
export enum OrderStatus {
  PENDING = "pending",
  FILLED = "filled",
  CANCELLED = "cancelled",
}

/**
 * * `market` - Market
 * * `limit` - Limit
 * * `stop_loss` - Stop Loss
 */
export enum OrderCreateOrderType {
  MARKET = "market",
  LIMIT = "limit",
  STOP_LOSS = "stop_loss",
}

/**
 * * `buy` - Buy
 * * `sell` - Sell
 */
export enum OrderCreateSide {
  BUY = "buy",
  SELL = "sell",
}

/**
 * * `market` - Market
 * * `limit` - Limit
 * * `stop_loss` - Stop Loss
 */
export enum OrderCreateRequestOrderType {
  MARKET = "market",
  LIMIT = "limit",
  STOP_LOSS = "stop_loss",
}

/**
 * * `buy` - Buy
 * * `sell` - Sell
 */
export enum OrderCreateRequestSide {
  BUY = "buy",
  SELL = "sell",
}

/**
 * * `market` - Market
 * * `limit` - Limit
 * * `stop_loss` - Stop Loss
 */
export enum OrderRequestOrderType {
  MARKET = "market",
  LIMIT = "limit",
  STOP_LOSS = "stop_loss",
}

/**
 * * `buy` - Buy
 * * `sell` - Sell
 */
export enum OrderRequestSide {
  BUY = "buy",
  SELL = "sell",
}

/**
 * * `market` - Market
 * * `limit` - Limit
 * * `stop_loss` - Stop Loss
 */
export enum PatchedOrderRequestOrderType {
  MARKET = "market",
  LIMIT = "limit",
  STOP_LOSS = "stop_loss",
}

/**
 * * `buy` - Buy
 * * `sell` - Sell
 */
export enum PatchedOrderRequestSide {
  BUY = "buy",
  SELL = "sell",
}

