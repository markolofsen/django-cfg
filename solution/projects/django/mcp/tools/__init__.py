"""
Project MCP Tools

Custom tools registered here are auto-discovered at Django startup.
Place your project-specific tools in this directory.

Each file should register its tools via:
    from django_cfg.modules.django_mcp.tools.base import tool_registry
    tool_registry.register(MyCustomTool())
"""
