"""TS extras: zod schemas + SWR hooks + events.ts.

Ported from cmdop_server/src/devtools/generator/tools/ts_extras/. Phase 4
copies the ported files into this directory; in Phase 1 we keep the package
empty so imports of the public surface succeed.
"""
