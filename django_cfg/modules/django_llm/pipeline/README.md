# pipeline

Placeholder package for decorator layers that wrap the LLM client.

Planned layers:

- **retry** — transparent retry with backoff on transient provider errors
- **router** — cascading model-chain selection
- **cache** — request/response caching policy
- **cost** — spend tracking and budget enforcement

Nothing is implemented here yet.
