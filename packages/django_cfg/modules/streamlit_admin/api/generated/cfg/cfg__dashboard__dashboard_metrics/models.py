# Auto-generated by DjangoCFG - see CLAUDE.md

from __future__ import annotations
from typing import Any

from pydantic import BaseModel, ConfigDict, Field


class MetricsResponse(BaseModel):
    """
    Complete metrics API response serializer. Root response containing all
    metric categories.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    categories: dict[str, Any] = Field(description='Metric categories (llm_balances,...')
    metadata: dict[str, Any] = Field(description='Response metadata (timestamp, co...')



